#include <stdio.h>

int main() {
    int tableau[] = {1, 2, 3, 4, 5};
    int taille = 5;
    int somme = 0;

    for (int i = 0; i < taille; i++) {
        somme += tableau[i];
    }

    // Affiche le résultat
    printf("La somme est : %d\n", somme);

    return 0;
}
